## 账单长截图 OCR → Excel `v1.0.1`

修复 **`启动.bat` 无法启动程序**、以及部分电脑上**浏览器不自动打开**的问题。建议所有 v1.0.0 用户升级。

---

### 下载

| 文件 | 说明 |
| :--- | :--- |
| [`bill-screenshot-ocr-v1.0.1-win64.zip`](https://github.com/Dsimpson-man/bill-screenshot-ocr/releases/download/v1.0.1/bill-screenshot-ocr-v1.0.1-win64.zip) | Windows 64 位 · 免安装 · 已内置 OCR 模型（约 96 MB） |

SHA256：`00849cf0d6e58aeb179e72a176f12b6ff146faaa4041c7db56c2f3419b8db40b`

> 解压后请**保留整个文件夹**：`_internal` 目录里是 Python 运行时和 OCR 模型，单独把 `exe` 拖出来无法运行。
> 也不要在压缩包窗口里直接双击 `启动.bat`，要先完整解压。

---

### 修复内容

**1. 启动脚本彻底修好（这是主要问题）**

`启动.bat` 之前被保存成 **UTF-8 无 BOM + LF 换行**，在中文 Windows 上 cmd.exe 按 ANSI(936) 读取会乱码，脚本里那句 `chcp 65001` 又会在读取途中切换代码页，导致 cmd **解析错位、只执行文件的一小部分** —— 实测连 `echo` 都被拆成 `'cho' 不是内部或外部命令`，**程序根本没被调用**。

现在改为 **GBK(cp936) + CRLF，并去掉 `chcp`**。同时启动脚本的逻辑部分刻意不含中文：不再写死中文目录名，改为按 `_internal` 标记自动定位 exe（找不到再回退到固定路径）。

`setup.bat` / `run.bat` 有同样的问题，一并修复。

**2. 浏览器唤起更可靠**

原来只调 `webbrowser.open()`，失败时被 `except: pass` **静默吞掉**，所以「服务起来了但页面没弹出来」时完全没有线索。现在逐级降级：

```
webbrowser.open()  →  os.startfile()  →  cmd start
```

每一步都会把结果（成功 / 失败原因）**打印到窗口里**，三种都失败才提示手动访问 `http://127.0.0.1:8848`。

**3. 日志实时可见**

给 stdout/stderr 开了行缓冲。之前输出被重定向时是块缓冲，诊断信息会一直攒着不显示。

**4. 使用说明补充排错**

新增「双击一闪就没了 / 提示找不到程序文件」和「浏览器没自动打开」两条，第一条最常见的原因是**没解压就直接在压缩包窗口里双击**。

---

### 校验

想要源码运行或了解算法细节，见 [README](https://github.com/Dsimpson-man/bill-screenshot-ocr#readme)。
