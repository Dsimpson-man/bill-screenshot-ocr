# 统一换行符，避免跨平台协作时出现无意义的整文件 diff
* text=auto eol=lf

# Windows 批处理必须用 CRLF，否则可能无法执行
# 另外：本项目的 .bat 一律保存为 GBK(cp936) 编码，不要改成 UTF-8、也不要加 chcp 65001
# —— cmd.exe 在读取过程中切换代码页会解析错位，脚本只会执行一部分（详见 run.bat 头部注释）
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.onnx binary
*.xlsx binary
