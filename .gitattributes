# 统一换行符，避免跨平台协作时出现无意义的整文件 diff
* text=auto eol=lf

# Windows 批处理必须用 CRLF，否则可能无法执行
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.onnx binary
*.xlsx binary
