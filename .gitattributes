# 统一换行符，避免跨平台协作时出现无意义的整文件 diff
* text=auto eol=lf

# Windows 批处理必须用 CRLF，否则可能无法执行
# 另外：本项目的 .bat 一律保持纯 ASCII（不含中文），不要加 chcp 65001
# —— cmd.exe 在读取途中切换代码页会解析错位，脚本只会执行一部分；
#    而含中文的脚本在 GitHub 上还会被转码，clone 下来同样会坏（详见 README）
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.onnx binary
*.xlsx binary
